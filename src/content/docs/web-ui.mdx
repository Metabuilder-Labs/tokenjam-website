---
title: Web UI
description: A local dashboard at http://127.0.0.1:7391 with status, traces, cost, alerts, budget, and drift views.
section: cli-and-ui
order: 2
---

`tj serve` starts a local dashboard at `http://127.0.0.1:7391/`.

```bash
tj serve
```

The web UI is a single Vite-built bundle served by the local REST API. No external dependencies, no cloud calls.

## Views

<table>
<tr>
<td width="50%">

**Status** — agent overview with cost, tokens, tool calls, and active alerts.

![tj status page](/docs/web-ui/tj-status.png)

</td>
<td width="50%">

**Traces** — recent traces with cost, duration, and span count. Click a row for the waterfall view.

![tj traces page](/docs/web-ui/tj-traces.png)

</td>
</tr>
<tr>
<td width="50%">

**Cost** — spend broken down by day, agent, model, or tool.

![tj cost page](/docs/web-ui/tj-cost.png)

</td>
<td width="50%">

**Alerts** — full alert history with severity filter and inline detail expansion.

![tj alerts page](/docs/web-ui/tj-alerts.png)

</td>
</tr>
<tr>
<td colspan="2">

**Budget** — view and edit daily/per-session cost limits per agent, with recent budget alerts inline.

![tj budget page](/docs/web-ui/tj-budget.png)

</td>
</tr>
</table>

**Drift** — behavioral drift report with Z-score analysis. Compares the latest session against a learned baseline. (Same page chrome as the others; reach via the Drift tab in the sidebar.)

## Auto-polling

The UI auto-refreshes every few seconds while a session is active. Disable from the settings menu if you'd rather refresh manually.

## Sharing

By default `tj serve` binds to `127.0.0.1` (loopback only). To make the dashboard accessible from another machine on your network:

```bash
tj serve --host 0.0.0.0
```

Authentication can be enabled in [Configuration](/docs/configuration). The ingest secret is separate from UI auth.

No signup, no cloud. Runs entirely on your machine.
